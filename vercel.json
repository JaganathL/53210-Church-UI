{
    "version": 2,
    "buildCommand": "npm run build",
    "outputDirectory": "dist/church-management",
    "framework": "angular",
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/.*",
            "dest": "/index.html"
        }
    ]
}